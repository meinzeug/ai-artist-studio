# Generate with: node --input-type=module -e "import {randomBytes} from 'node:crypto'; console.log(randomBytes(32).toString('hex'))"
POSTGRES_PASSWORD=
RUNNER_TOKEN=
TOKEN_ENCRYPTION_KEY=
APP_ORIGIN=http://127.0.0.1:3210
TIKTOK_CLIENT_KEY=
TIKTOK_CLIENT_SECRET=
TIKTOK_REDIRECT_URI=

# Required one-time setup protection when APP_ORIGIN uses HTTPS.
SETUP_TOKEN=
